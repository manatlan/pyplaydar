Here is a resolver which is able to use the tomahawk js resolvers
You'll need to install V8 and pyV8 !

Use the provided utility "tomres.py" to install/uninstall/list tomahawk js resolvers,
Example :

    $ ./tomres.py install

will install the resolvers here
